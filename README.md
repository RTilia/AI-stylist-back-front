# 👗 AI Stylist

AI Stylist — это полноценное веб-приложение умного персонального стилиста. Проект состоит из бэкенда на Python (FastAPI) и фронтенда на JavaScript (Vite), работающих вместе, чтобы обеспечить персонализированный подбор гардероба с использованием нейросетей.

## 🌟 Возможности
- Интеллектуальный анализ стиля, определение цветотипа и типа фигуры по анкете и фото.
- Генерация капсульного гардероба на основе предпочтений и цели (повседневный, офис, свидание и т.д.).
- Поиск реальных товаров на Wildberries по сгенерированным рекомендациям.
- Личный кабинет с сохранением параметров и истории сгенерированных образов (капсул).
- Удобное кликабельное «Избранное» для товаров и целых образов.

---

## 📂 Структура проекта

Репозиторий разделен на две основные части:
- `ai-stylist-backend/` — Серверная часть (Python, FastAPI, интеграция с нейросетью OpenRouter и парсинг Wildberries).
- `ai-stylist-frontend/` — Клиентская часть (Vite, чистый JS/HTML/CSS).

---

## 🛠 Запуск проекта локально

Для работы проекта необходимо запустить оба сервера (бэкенд и фронтенд) параллельно в двух разных терминалах.

### 1. Запуск Бэкенда (API)
Бэкенд отвечает за логику, нейросети и базу данных. Требуется Python 3.9+.

```bash
cd ai-stylist-backend

# 1. Создание и активация виртуального окружения
python -m venv venv
# Для Windows:
venv\Scripts\activate
# Для macOS/Linux:
# source venv/bin/activate

# 2. Установка зависимостей
pip install -r requirements.txt

# 3. Настройка окружения
# Создайте файл .env внутри папки ai-stylist-backend и добавьте ваш ключ:
# OPENROUTER_API_KEY="ваш_ключ"

# 4. Запуск сервера
python main.py
```
*Бэкенд запустится на `http://127.0.0.1:8000` (Документация API: `http://127.0.0.1:8000/docs`)*

### 2. Запуск Фронтенда (Интерфейс)
Фронтенд предоставляет пользовательский интерфейс. Требуется Node.js 18+.

```bash
cd ai-stylist-frontend

# 1. Установка зависимостей
npm install

# 2. Запуск сервера разработки
npm run dev
```
*Фронтенд запустится на ссылке, показанной в терминале (обычно `http://localhost:5173`).*

---

## ⚙️ Основные технологии
- **Backend:** FastAPI, SQLAlchemy (SQLite), OpenAI SDK (OpenRouter: `nvidia/nemotron-nano-12b-v2-vl:free`).
- **Frontend:** Vite, HTML5, Vanilla JavaScript, CSS3 (без сторонних UI-фреймворков).

## 📝 Заметки
- **База знаний:** В папке `ai-stylist-backend/knowledge_base/` находятся текстовые промпты и инструкции, которые задают поведение ИИ-стилиста. Не удаляйте их.
- **Данные пользователей:** Сохраняются локально в `sql_app.db` (база данных SQLite). Избранное частично сохраняется в `localStorage` браузера.
